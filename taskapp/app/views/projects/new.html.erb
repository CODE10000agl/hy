<h1>Add New</h1>

<%= form_for @project do |f| %>
 
<p>
	<%= f.label :title %> <br><br>
	<%= f.text_field :title %>
	<%= if @project.errors.any? %>
	<%= @project.errors.inspect %>
	<% end%>
</p>

<p>
	<%= f.submit %>
</p>

<% end %>